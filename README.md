✅ Как использовать:
Распакуйте архив с файлами.
Откройте chrome://extensions/ → ✅ Режим разработчика → Загрузить распакованное расширение.
Перейдите на целевой сайт.
Кликните по значку расширения → Соберётся статистика автоматически.
Нажмите «Показать таблицу» → увидите уникальные FQDN.
Экспортируйте в CSV.
🔐 Безопасность и конфиденциальность
Расширение не отправляет данные никуда — всё хранится в chrome.storage.local.
Работает только при открытом popup (т.е. не фоново), но сбор идёт всегда (можно ограничить onActivated + onUpdated, если нужно).
Для продвинутого контроля можно добавить фильтр по типу ресурса (details.type: script, image, xhr, websocket и т.п.).
📌 Дополнительно по вашим пунктам:
✅ wss/http/https/smb — протокол берётся из url.protocol.
✅ Количество вхождений — считаем в count.
✅ Расширение файла — извлекаем из pathname.
✅ Сортировка — реализована по клику.
✅ Экспорт в CSV — работает "из коробки".
